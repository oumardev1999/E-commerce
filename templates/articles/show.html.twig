{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
   
<div class="container mt-5  py-5 class="bg-image" style="background-image: url('https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRXJt53AigUsY_bT5VEVeBNkCPd8U_DXuRHIA&usqp=CAU')">
 <div class="card">
 <div class="card-header text-danger text-center">
  <h class="text-center"1>Article</h1>
 </div>
  <div class="card-body">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_articles_index') }}">back to list</a> #}

    {# <a href="{{ path('app_articles_edit', {'id': article.id}) }}">edit</a> #}

    {# {{ include('articles/_delete_form.html.twig') }} #}
    <div>
     <a href="{{path('app_comment_new',{'id': 5})}}">Ajouter un commentaire</a>
    </div> 
    <div>
   {% for comment in comments %}
<div> {{comment.user.email}} a dit: </div>
<div>{{comment.Content }} </div>
   {% endfor %}
    </div> 
  </div>
</div>
</div>

   
{% endblock %}